{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo '==> PORT=$PORT DB=$DATABASE_URL' && timeout 15 alembic upgrade head || echo '==> Alembic skipped/failed' && echo '==> Starting uvicorn...' && exec uvicorn app.main:app --host 0.0.0.0 --port $PORT --log-level info",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
