{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo '==> Running alembic...' && alembic upgrade head && echo '==> Alembic done. PORT=$PORT. Starting uvicorn...' && uvicorn app.main:app --host 0.0.0.0 --port $PORT --log-level info",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
